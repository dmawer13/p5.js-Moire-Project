function setup() {
  // put setup code here
  createCanvas(100,100);
  background(155);
}

function draw() {
  // put drawing code here
  ellipse(width/2, height/2, 10,10);
}

// \/ code by PD:
// function isThisAllowed() {
//   rect(mouseX, mouseY, 10, 10)
// https://atom.io/packages/atom-html-preview
// open browser preview with CTRL SHIFT H (windows)
// }
